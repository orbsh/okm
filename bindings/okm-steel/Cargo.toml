[package]
name = "okm-steel"
version = "0.1.0"
edition = "2021"

[dependencies]
okm-core = { path = "../../okm-core", features = ["schema-serde"] }
okm-dynamic = { path = "../../okm-dynamic" }
steel-core = "0.7"
serde_json = "1"
im-rc = "15"

# standalone crate — keep out of the okm root workspace
[workspace]

[dev-dependencies]
okm-derive = { path = "../../okm-derive" }
